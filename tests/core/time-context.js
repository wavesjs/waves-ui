const assert = require('assert');
const TimeContext = require('../../es6/core/time-context');

describe('TimeContext', function(){
    describe('instanciation, getters and setters', function(){
        let tc = new TimeContext();
        // not HERE offsetX don't change when stretchRatio change ...
        // change scale on layer (and timeline) and check that offset don't move
    })
})
